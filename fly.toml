# fly.toml configuration for GoRent deployment on Fly.io
app = "go-rent"  # choose a unique name; change if already taken
primary_region = "cdg"  # Paris region (adjust as needed)

[env]
  PORT = "8080"
  # Database connection – Fly will inject DATABASE_URL if you attach a Postgres app
  DB_HOST = "localhost"
  DB_PORT = "5432"
  DB_USER = "postgres"
  DB_PASSWORD = "Meta-Ma@2021"
  DB_NAME = "rentmaroc"

[services]
  [[services.ports]]
    port = 80
    handlers = ["http"]

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"
    grace_period = "30s"
